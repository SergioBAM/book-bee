Read AGENTS.md and docs/.

Fix the app shell so the Shelf / Scan / Wishlist bottom navigation is not obscured by Android system navigation buttons.

Use Compose/system insets correctly.
Do not add new features.
Do not redesign the app.
Keep the change small and focused.

After the change, run ./gradlew assembleDebug and report the result.
